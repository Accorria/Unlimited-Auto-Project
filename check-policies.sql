-- Check if there are any active policies on the dealers table
-- Run this in Supabase SQL editor

SELECT 
    schemaname,
    tablename,
    policyname,
    permissive,
    roles,
    cmd,
    qual,
    with_check
FROM pg_policies 
WHERE schemaname = 'public' 
AND tablename = 'dealers';
